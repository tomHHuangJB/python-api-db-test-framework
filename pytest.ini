[pytest]
testpaths = tests
addopts = -q
markers =
    smoke: quick validation suite
    negative: negative test cases and error handling
    security: security-focused tests
    critical: critical user-flow scenarios
    regression: regression suite
